#-------------------------------------
# NextAuth Core Configuration
#-------------------------------------
# Used to sign and encrypt JWTs, session cookies, etc. Generate with 'openssl rand -base64 32'
NEXTAUTH_SECRET=
# The canonical URL of your application (e.g., http://localhost:3000 for local dev)
NEXTAUTH_URL=http://localhost:3000

#-------------------------------------
# Database (Prisma)
#-------------------------------------
DATABASE_URL="file:./dev.db" # Or your actual connection string

#-------------------------------------
# Azure Active Directory (for NextAuth Login)
#-------------------------------------
AZURE_AD_CLIENT_ID= # Application (client) ID from your Azure AD App Registration (GUID)
AZURE_AD_CLIENT_SECRET= # Client Secret from your Azure AD App Registration
AZURE_AD_TENANT_ID= # Directory (tenant) ID from your Azure AD

#-------------------------------------
# Azure OpenAI Service (Primary for LLM Chat, Embeddings, Prompt Gen)
#-------------------------------------
# Common endpoint for your Azure OpenAI resource
AZURE_OPENAI_ENDPOINT=https://YOUR_AOAI_RESOURCE_NAME.openai.azure.com/
# API Key for your Azure OpenAI resource
AZURE_OPENAI_API_KEY=
# Deployment name for your main Chat/Generation model (e.g., gpt-4, gpt-35-turbo)
AZURE_DEPLOYMENT_NAME=
# Deployment name for your Text Embedding model
AZURE_EMBEDDING_DEPLOYMENT_NAME=
# Your Azure OpenAI resource name (e.g., "my-aoai-service" - often part of the endpoint)
AZURE_RESOURCE_NAME= 

# Note: If using the same AOAI resource for prompt generation actions, the following can be redundant
# or set to the same values as above. If it's a dedicated/different model/service for prompt gen:
AZURE_PROMPT_GEN_ENDPOINT=${AZURE_OPENAI_ENDPOINT} # Or specific if different
AZURE_PROMPT_GEN_API_KEY=${AZURE_OPENAI_API_KEY}    # Or specific if different
AZURE_PROMPT_GEN_DEPLOYMENT_NAME=${AZURE_DEPLOYMENT_NAME} # Or specific model if different

#-------------------------------------
# Azure AI Search (for RAG)
#-------------------------------------
AZURE_SEARCH_ENDPOINT=https://YOUR_AI_SEARCH_SERVICE_NAME.search.windows.net
AZURE_SEARCH_KEY= # Admin Key for your Azure AI Search service
AZURE_SEARCH_INDEX_NAME=your_index_name
AZURE_SEARCH_CONTENT_FIELD=content # Field in your search index containing the main text
AZURE_SEARCH_VECTOR_FIELD=embedding # Optional: Field name for embeddings, defaults to "embedding"
# AZURE_SEARCH_SEMANTIC_CONFIGURATION_NAME= # Optional: Name of your semantic configuration in Azure AI Search

#-------------------------------------
# Azure Storage (for Document Blobs)
#-------------------------------------
# Primary way: Connection string (often preferred)
AZURE_STORAGE_CONNECTION_STRING="DefaultEndpointsProtocol=https;AccountName=YOUR_STORAGE_ACCOUNT_NAME;AccountKey=YOUR_STORAGE_ACCOUNT_KEY;EndpointSuffix=core.windows.net"
# If using connection string, the individual name/key below might be for specific SDKs or redundant
AZURE_STORAGE_ACCOUNT_NAME= # Your Azure Storage account name
AZURE_STORAGE_ACCOUNT_KEY=  # A key for your Azure Storage account
AZURE_STORAGE_CONTAINER_NAME=your_blob_container_name # Container where original documents are stored

#-------------------------------------
# Azure Document Intelligence (for processing uploaded files)
#-------------------------------------
AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://YOUR_DOC_INTEL_SERVICE_NAME.cognitiveservices.azure.com/
AZURE_DOCUMENT_INTELLIGENCE_KEY=

#-------------------------------------
# Node Environment (usually 'development' or 'production')
#-------------------------------------
NODE_ENV=development